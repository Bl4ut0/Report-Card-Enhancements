# Changelog

## Unreleased

- Added initial Warcraft Logs Proxy project scaffold.
- Added Cloudflare Worker with shared-secret validation, Warcraft Logs target allowlisting, bounded retries, `Retry-After` handling, and optional GET caching.
- Added Apps Script helper example for routing WCL fetches through the Worker.

